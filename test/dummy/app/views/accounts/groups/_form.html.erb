<%= form_with(model: accounts_group) do |form| %>
  <% if accounts_group.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(accounts_group.errors.count, "error") %> prohibited this accounts_group from being saved:</h2>

      <ul>
        <% accounts_group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
