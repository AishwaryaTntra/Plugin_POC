<%= form_with(model: basic_group) do |form| %>
  <% if basic_group.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(basic_group.errors.count, "error") %> prohibited this basic_group from being saved:</h2>

      <ul>
        <% basic_group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
